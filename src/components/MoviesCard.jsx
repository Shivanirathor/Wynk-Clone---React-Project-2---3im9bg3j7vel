import React from 'react'

const MoviesCard = ({title}) => {
  return (
    <div>
<div>title: {title}</div>
<div>title: {title}</div>
<div>title: {title}</div>
    </div>
  )
}

export default MoviesCard